# Setup of gcp-doctor-repo project

- Created project
- Enabled "Artifact Registry" API
- Created Docker repository "dev", multi-region US
- Added 'Artifact Registry Reader' access to 'allUsers'
- Configured docker:

  ```
  gcloud beta auth configure-docker us-docker.pkg.dev
  ```
